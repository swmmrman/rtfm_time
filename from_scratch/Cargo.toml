[package]
name = "from_scratch"
version = "0.1.0"
edition = "2024"

[dependencies]
cortex-m = { version = "0.7.7" }
cortex-m-rt = "0.7.5"

embassy-executor = { version = "0.9", features = [
    "arch-cortex-m",
    "executor-thread"
] }
